<h1>投稿一覧</h1>
<% @titles.each do |title| %>
  <p>タイトル</p>
  <span>
    <%= link_to title.name, new_post_path(title.id) %>
  </span>
  <p>画像</p>
  <%= attachment_image_tag title, :image, :fill, 100, 100, format: 'jpeg' %>
<% end %>